import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  likesContainer: {
    position: 'absolute',
    bottom: 60,
    flexDirection: 'row',

    alignItems: 'center',
    justifyContent: 'center',
  },
});
