import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  overlayContainer: {
    flex: 1,
  },
  companiesContainer: {
    justifyContent: 'center',
    alignItems: 'center',
    marginHorizontal: 20,
    marginTop: 10,
  },
});
