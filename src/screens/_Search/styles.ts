import { StyleSheet, Dimensions } from 'react-native';

export default StyleSheet.create({
  overlayContainer: {
    flex: 1,
  },
  top: {
    height: 75,
    alignItems: 'center',
    position: 'absolute',
    justifyContent: 'center',
    width: '100%',
    bottom: 10,
  },
});
